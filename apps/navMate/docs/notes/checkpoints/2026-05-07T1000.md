# Checkpoint 2026-05-07T1000

**PRIMARY CLAUDE ONLY — /api/nmdb response updated.**

---

## What this session accomplished (since T0930)

One code change only.

---

## Change applied

### nmServer.pm — /api/nmdb returns visible and versioning columns

Four SQL queries behind the `/api/nmdb` endpoint updated:

- **collections**: added `visible`
- **waypoints**: added `visible`, `db_version`, `e80_version`, `kml_version`
- **routes**: added `visible`, `db_version`, `e80_version`, `kml_version`
- **tracks**: added `visible`, `db_version`, `e80_version`, `kml_version`

`route_waypoints` unchanged (no visibility or versioning columns in schema).

---

## State

Patrick still editing nmOperations.md independently.
No implementation has started. Next session continues wherever nmOperations.md review lands.
