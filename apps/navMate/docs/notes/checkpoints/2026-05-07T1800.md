# Checkpoint 2026-05-07T1800

## Work completed this session

### nmOps_testplan.md -- written from scratch

Produced `apps/navMate/docs/notes/nmOps_testplan.md` by synthesizing three source
documents: nmOperations.md (new design), context_menu.md (old design), and
context_menu_testplan.md (old test plan). Key knowledge preserved from the old sources:

**From context_menu_testplan.md:**
- Database shape requirements structure and rationale (isolated WPs, groups with/without
  route refs, branch safety, branch-with-routes-and-groups ordering dependency)
- Track UUID behavior: E80->DB paste assigns a fresh UUID; search by name not UUID;
  color conversion table (E80 index -> aabbggrr)
- The `my_waypoints` node key quirk: no `header:` prefix; `header:my_waypoints` fails
- §3.0 WP-upload pitfall: use `header:groups` not `my_waypoints` when E80 has no
  ungrouped WPs yet
- Progress dialog polling pattern (bounded 20-iteration poll with beep on timeout)
- Reset sequence: suppress must be enabled BEFORE any E80 delete/clear commands
- last_testrun.md format: PASS/FAIL/PARTIAL/PASSED_BUT/NOT_RUN; Issues section always
  present; triage guide format

**From context_menu.md:**
- Node key format table including `rp:ROUTE_UUID:WP_UUID` for route points
- `header:groups`, `header:routes`, `header:tracks`, `my_waypoints`, `root`
- /api/db vs /api/nmdb structure (hashes vs arrays; different parent field names)
- Progress dialog dispatch guard behavior (next /api/test is silently skipped if dialog
  is still active -- always wait for idle)
- Log reading perl one-liner

**New content from nmOperations.md design:**
- Unified COPY=10010 / CUT=10110 -- per-type constants (10011, 10020, 10030, etc.)
  are gone entirely; this is the biggest behavioral change for the runbook
- PASTE_BEFORE/AFTER/PASTE_NEW_BEFORE/PASTE_NEW_AFTER (10302-10305) as first-class test
  cases (§2.13 collection positional insertion; §2.14 route sequence reordering;
  §3.16 E80 route point insertion)
- E80 header-node Delete tests (§3.5-3.8): Groups header, Routes header, My Waypoints,
  Tracks header -- all four SS8.2 rules now have explicit test steps
- Pre-flight failure paths as first-class tests (§5), not just a guard section at the
  end; 13 steps covering route dependency, ancestor-wins, recursive paste, name collision
  (intra-clipboard and E80-wide), UUID conflict paths
- PREREQUISITE markers on two §5 tests that require outcome=reject support

### todo.md -- item #3 marked done, item #4 expanded

Item #3 (Write nmOps_testplan.md) marked DONE.

Item #4 replaced with six concrete sub-items identifying the API and code changes
required before the testplan can be fully executed:

1. **$suppress_outcome control** -- the most critical; three two-outcome dialog sites
   need to be testable on both the accept and reject paths: ancestor-wins confirmation
   (SS6.2), UUID conflict choice (SS10.10), and E80 DEL-WP route-ref warning (SS8.2).
   Proposal: `outcome=accept|reject` param on `op=suppress` endpoint.
2. **Route point key in nmTest.pm tree walker** -- `rp:ROUTE_UUID:WP_UUID` must resolve.
3. **PASTE_BEFORE/AFTER dispatch** -- 10302-10305 not in old context_menu system; may
   not yet be wired in nmTest.pm dispatch.
4. **Ancestor-wins dialog through suppress** -- new SS6.2 dialog must route through
   $suppress_confirm or every paste with ancestor-wins resolution will hang.
5. **UUID conflict dialog through suppress** -- same issue for SS10.10 conflict choice.
6. **E80 Tracks header Delete** -- SS8.2 new capability; verify implemented.

## State of nmOps_testplan.md

Written and complete as a forward-looking document. It assumes the feature implementation
(module structure, PASTE_BEFORE/AFTER, new constants) is done. Tests requiring
prerequisites not yet built are marked PREREQUISITE or REQUIRES_SETUP.

The document does NOT contain the UUID table (that belongs in nmOps_runbook.md) but
describes the shape requirements that the runbook UUID table must satisfy, including a
new requirement: route with at least 3 ordered route_waypoints for the
PASTE_BEFORE/AFTER sequence tests.

## Outstanding / next

- nmOps_runbook.md: not yet written; next step after testplan; builds the UUID table
  and provides exact curl commands with [Name] placeholders resolved to actual UUIDs
- [Rework operations system] todo steps 1-2 (module structure decision, feature
  implementation) must precede the runbook
- The six testability prerequisites in todo.md item #4 must be completed before
  automated test cycle execution
