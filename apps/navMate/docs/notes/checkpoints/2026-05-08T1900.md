# Checkpoint 2026-05-08T1900

## Session summary

Continuing primary Claude from 2026-05-08T1700 checkpoint. Completed §5.9 through §5.16,
wrote last_testrun.md (Cycle 8 complete), updated testplan and runbook. Cycle 8 test run
is fully documented. No code changes made this session.

---

## What was done

- §3.4 bug fix (from previous pre-clear): `_pasteOneWaypointToDB` e80-source branch added;
  E80-source paste now always overwrites existing DB record unconditionally.
- §5.9-§5.16 run to completion (see last_testrun.md for full results)
- last_testrun.md rewritten as Cycle 8 (was mislabeled Cycle 7 from prior session)
- testplan §3.4 "known bug" note updated to "fixed"
- runbook updated: COPY+PASTE timing rule, §3.18 WP conflict warning, §3.7/§3.8 note
- session_state.md updated with Cycle 8 checkpoint

---

## Cycle 8 test results summary

- §1 PASS
- §2.1-§2.18 ALL PASS
- §3 PARTIAL: §3.0c FAIL (0 route_waypoints), §3.11 FAIL (homogeneity), §3.14 FAIL
  (route name collision), §3.15 FAIL (BOCAS2 seq collision); §3.8/§3.16 NOT_RUN
- §4 NOT_RUN (teensyBoat)
- §5: 7 PASS, 7 FAIL, 2 NOT_RUN
  - PASS: §5.2, §5.3, §5.5, §5.7, §5.10, §5.11, §5.12
  - FAIL: §5.1, §5.4, §5.6, §5.9, §5.14, §5.15, §5.16
  - NOT_RUN: §5.8, §5.13

10 bugs documented in last_testrun.md Issues section.

---

## New bugs found this continuation (§5.14, §5.15, §5.16)

**§5.9:** Recursive paste guard only checks `item.uuid == dest_uuid`, NOT whether destination
is a descendant of the clipboard item. Fix: walk ancestor chain from dest upward.

**§5.14:** PASTE and PASTE_NEW at DB object nodes (waypoint/route/track) silently no-op.
`_pasteDB` uses `$right_click_node->{data}{uuid}` as collection target regardless of node
type. Fix: check `$right_click_node->{type}` == 'collection' before dispatching.

**§5.15:** PASTE at individual E80 WP node succeeds (no guard). PASTE_NEW at same node
attempts create, fails at WPMGR (-1 from E80), leaves hung ProgressDialog.
Fix: check destination node type in `_pasteE80`; also fix WPMGR error path to call
`_closeE80Progress` on failure.

**§5.16 sub-1:** PASTE_BEFORE with mixed clipboard (route_point + waypoint) succeeds instead
of rejecting. Fix: add purity check in `_pasteDB` for PASTE_BEFORE route-sequence path.

**§5.16 sub-2:** PASTE_NEW_BEFORE does not filter waypoints; inserts them via
`_insertFreshWaypoint`. Fix: skip non-route_point items in the fresh route-sequence path.

---

## State of E80 and DB at end of session

E80: wps=8 (ce4e43181f01b3ae=BOCAS1, af4e23246d01bfa8=BOCAS2 ungrouped + 6 Timiteo members),
     groups=1 (Timiteo, 104e0f64e30422ec), routes=0

DB: navMate.db in end-of-cycle-8 state. Changes accumulated:
- [DST] contains all §2 test objects
- TestRoute (f34efdd6070022e8) now has 20 route_waypoints (was 11; PASTE_BEFORE/AFTER tests
  added duplicates throughout §2 and §5.16 testing)
- MandalaLogs/Tracks (984e7898480427f6) has two stray MandalaLogs child branches from two
  §5.9 runs: db4e140825056ffe and 364e1250e404b52a

---

## Bugs requiring code fix + navMate restart (all deferred)

All from last_testrun.md Issues section. Key ones with proposed fixes:

1. **§3.0c / §5.6:** `_pasteRouteToE80` reads `$item->{members}` but route clipboard items
   use `$item->{route_points}`. Change key to `{route_points}`.

2. **§3.11:** Homogeneity check (nmOps.pm Step 6) rejects group+route mix after branch
   dissolution. Expand permitted two-type mixes to include group+route.

3. **§3.14:** Route PASTE_NEW does not apply name deconfliction (WP PASTE_NEW does via
   `_deconflictE80Name`). Apply same to routes.

4. **§5.1:** `getWaypointRouteRefCount` returned 0 for Popa0 (which IS in the Popa route).
   Investigate whether §2.14 PASTE_BEFORE/AFTER corrupted the route_waypoints entry.

5. **§5.4:** E80 Tracks header destination has no handler-level guard. Add destination-type
   check in `_pasteE80`.

6. **§5.9:** Recursive guard incomplete. Walk ancestor chain.

7. **§5.14:** No guard for DB object nodes. Check type before dispatching.

8. **§5.15:** No guard for individual E80 WP nodes. Also fix hung ProgressDialog on error.

9. **§5.16:** Mixed clipboard not rejected for PASTE_BEFORE; waypoints not filtered for
   PASTE_NEW_BEFORE.

---

## How to continue

1. Read this checkpoint and last_testrun.md
2. Present 10 bugs to Patrick for fix prioritization
3. Implement authorized fixes, restart navMate
4. Run Cycle 9 from §1 reset
