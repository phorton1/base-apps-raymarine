# Checkpoint 2026-05-06T1358

## Deferred Design Vision — winE80 / Leaflet Integration

Patrick articulated a cluster of related future work that he is explicitly NOT
addressing now. These should go into design_vision.md as a named cluster:

1. **Separate Leaflet layer for E80 items** — a Leaflet mode or layer set that
   shows the winE80 database (E80-native items) distinctly from the navMate DB items.

2. **Non-persistent visibility checkboxes on E80 items** — similar to the
   winDatabase visibility UI we built today, but for E80 items; non-persistent
   (session-only, not stored to the navMate DB).

3. **Editors for E80 items** — similar to the editors created today in winDatabase,
   applied to the E80 item set.

4. **Versioning / color synchronization** — the scheme for keeping navMate DB
   versions and E80 versions in sync, including the color model, across the two views.

5. **nmOps / context_menu complexity** — all of the above is entangled with the
   horridly complex context_menu nmOps scheme. Patrick is aware; deferred.

These are not independent items — they form one large future feature cluster.
Update Claude: add as a single named cluster to design_vision.md (not split into
separate todo items). Do not subsume or replace existing design_vision.md content
unless there is direct overlap.

## Immediate Next Work — winDatabase Reordering

Patrick wants to add UX to winDatabase for reordering items in the navMate
database. Scope is deliberately narrow: just the reordering capability within
winDatabase/the navMate DB. Nothing about E80 sync or the deferred cluster above.

No design decisions yet — we were about to look at the schema and winDatabase
code when Patrick interrupted to save this context. Current schema has no
explicit sort-order column on collections, waypoints, routes, or tracks.

Update Claude: add a todo item for "winDatabase reordering UX" in todo.md.
