# Checkpoint 2026-05-06T1700

**UPDATE CLAUDE: no doc or memory changes from this checkpoint. Primary Claude only.**

## Phase 1 Excision — Status: Nearly Complete, One Fix Needed

### What was done this session

**File operations:**
- Deleted outright: `nmUpload.pm`, `_e80_dedup.pm`, `nmOldE80.pm`
- Moved to `junk/`: `nmClipboard.pm`, `nmOps.pm`, `nmOpsDB.pm`, `nmOpsE80.pm`, `nmTest.pm`

**winDatabase.pm:**
- Removed `use nmUpload`, `use nmClipboard`, `use nmOps`
- Added local command IDs: `$CTX_CMD_DELETE=10562`, `$CTX_CMD_NEW_BRANCH=10563`, `$CTX_CMD_NEW_GROUP=10564`
- Replaced old EVT_MENU block with bindings for Delete, NewBranch, NewGroup, Show/Hide
- Rewrote `_buildContextMenu`: Delete + New Branch/Group + Show/Hide on Map only
- Added `_onDelete`, `_onNewBranch`, `_onNewGroup` as direct inline implementations using c_db.pm functions
- Removed `_onContextMenuCommand` and `_onUploadE80`
- Syntax check: **PASSES**

**winE80.pm:**
- Removed `use nmClipboard`, `use nmOps`
- Stripped `_buildContextMenu` to just Refresh E80
- Removed `_onContextMenuCommand`
- Syntax check: **PASSES**

**winMain.pm:**
- Removed `use nmOps`, `use nmOldE80`, `use nmTest`
- Added `use Pub::WX::ProgressDialog` — **THIS IS THE PROBLEM** (see below)
- Inlined `_doRefreshE80Data` (was `doRefresh` from nmOps.pm)
- Added stub `dispatchTestCommand {}` (replaces nmTest.pm export)
- `COMMAND_IMPORT_OLDE80` handler replaced with `warning(0,0,"no longer available")`
- Syntax check: **FAILS** on `use Pub::WX::ProgressDialog`

### One fix needed before commit

`Pub::WX::ProgressDialog` was already used in winMain.pm BEFORE my changes
(lines 98-99: `forceCloseActive`, `isActive`) without an explicit `use` statement.
It's loaded transitively through `use Pub::WX::Frame` (base class).
The explicit `use Pub::WX::ProgressDialog;` I added at line 22 is wrong —
remove it. The inlined `_doRefreshE80Data` will still work because the module
is already in memory via the Frame.

**Fix:** Remove line 22 `use Pub::WX::ProgressDialog;` from winMain.pm, then
re-run the syntax check. That should be the only remaining issue.

### After the fix: verify and commit

```
perl -c winMain.pm   # should pass after removing the use line
git add -A
git commit -m "Phase 1: excise nmOps scheme — move to junk, rewire winDatabase/winE80/winMain"
```
