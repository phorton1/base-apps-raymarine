# Checkpoint 2026-05-08T1300

## Status

Cycle 7 test run COMPLETE. last_testrun.md written. session_state.md updated.

## What was done this session

- Resumed test cycle at §3.5 (continuing from T1120 checkpoint)
- Ran §3.5 through §3.16, §5.1 through §5.13 (§4 NOT_RUN, teensyBoat unavailable)
- §5.10 and §5.11 run in this session (final two tests): both PASS
- last_testrun.md replaced with Cycle 7 full results

## Test cycle results summary

| Section | Result |
|---------|--------|
| §1 reset | PASS |
| §2 database (14 steps) | ALL PASS |
| §3 E80 | PARTIAL: 10 PASS, 1 PASSED_BUT, 3 FAIL, 2 NOT_RUN |
| §4 tracks | NOT_RUN (teensyBoat) |
| §5 guard | PARTIAL: 6 PASS, 4 FAIL, 3 NOT_RUN |

## 6 bugs found (no fixes applied -- awaiting Patrick authorization)

### Bug 1: route_points/members key mismatch (root cause of 3 failures)
- Affects: §3.0 PASSED_BUT, §3.16 NOT_RUN, §5.6 FAIL
- `_pasteRouteToE80` (nmOpsE80.pm:833) reads `$item->{members}` but route clipboard items
  are built with key `route_points` (nmOps.pm:298, 433)
- Every DB->E80 route paste creates an empty route with 0 waypoints
- Fix: change nmOpsE80.pm:833 to read `route_points`; update line 921 count too

### Bug 2: homogeneity check too strict (§3.11 FAIL)
- nmOps.pm:562 only allows waypoint+group as two-type mix; blocks group+route
- Branch-dissolved [RouteBranch] yields group+route mix -- rejected
- Fix: expand allowed two-type mix or remove the restriction for group+route

### Bug 3: route PASTE_NEW not deconflicted (§3.14 FAIL)
- WP PASTE_NEW uses _deconflictE80Name; route PASTE_NEW does not
- Step 7 fires on route PASTE_NEW when same-named route already on E80
- Fix: apply name deconfliction to routes in PASTE_NEW path

### Bug 4: Popa0 deleted by DEL_WAYPOINT (§5.1 FAIL)
- getWaypointRouteRefCount returned 0 for Popa0 (314e56cc09005332)
- WP was in Popa route but route_waypoints ref count returned 0
- Likely cause: route_waypoints corrupted during §2.14 PASTE_BEFORE/AFTER ops
- Needs investigation: was Popa0's route_waypoints row zeroed or removed?

### Bug 5: E80 tracks header paste not blocked (§5.4 FAIL)
- SS10.8 guard not enforced; BOCAS1 WP created ungrouped on E80 with tracks as destination
- Fix: check destination type before dispatching in _pasteE80 or _pasteAllToE80

### Bug 6: recursive paste guard incomplete (§5.9 FAIL)
- nmOps.pm Step 3 only checks `$item->{uuid} eq $dest_uuid`
- Does not check if dest is a descendant of the clipboard item
- Pasting MandalaLogs (234e412e3104296e) into MandalaLogs/Tracks (984e7898480427f6): new branch created
- Fix: walk DB ancestry from dest_uuid upward; abort if any ancestor UUID matches clipboard item UUID

## E80 state at end of session

Timiteo group + 6 WPs (fresh UUIDs from §5.7) + BOCAS1 WP (ce4e43181f01b3ae from §5.11 setup).
DB state: test modifications throughout §2 (SafeBranch deleted, Routes/Tracks moved to DST, etc).
navMate.db is NOT at git baseline -- was modified by test cycle.

## Next

Patrick reviews 6 bugs above and decides which to fix and in what order.
After fixes, Cycle 8 test run needed to verify.
