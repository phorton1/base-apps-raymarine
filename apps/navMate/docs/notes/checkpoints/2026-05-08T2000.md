# Checkpoint 2026-05-08T2000

## Session context

Fresh primary Claude session after /clear. Picked up from T1900 checkpoint.
Started with "do the 3.0c bug fix" request; diverted to fix a documentation
sync problem first.

---

## What was done this session

### last_testrun.md numbering fixed

The Cycle 8 last_testrun.md was written by the previous session using old
§3.0a/§3.0b/§3.0c numbering instead of the runbook's §3.1/§3.2/§3.3 numbering.
Root cause: the previous Claude wrote from context/memory rather than copying
section numbers from the runbook.

**Two fixes applied:**

1. Runbook (`nmOps_testplan_runbook.md`) -- added explicit constraint at the
   top of the `last_testrun.md format` section: section numbers must match the
   runbook exactly; the runbook is the canonical source.

2. last_testrun.md -- all §3 numbers corrected to match runbook:
   - §3.0a/§3.0b/§3.0c → §3.1/§3.2/§3.3
   - Old §3.1-§3.10 → §3.4-§3.13
   - Old §3.11 (DB→E80 all-paste [RouteBranch]) -- DROPPED from table; this
     test was confirmed in T1439 as a test-design error (homogeneity check
     correctly blocked it); the test no longer exists in the testplan. Summary
     updated: 12 PASS, 1 PASSED_BUT, 3 FAIL, 2 NOT_RUN.
   - Old §3.12-§3.16 → §3.15-§3.19
   - Internal cross-references updated: §3.17 note "from §3.11" corrected to
     "from §3.3"; §3.18 note "from §3.12" corrected to "from §3.15"; §5.12
     "Covered by §3.12" corrected to "Covered by §3.15".

---

## Critical finding: §3.3 bug analysis in last_testrun.md is STALE

The Issues section for §3.3 (was §3.0c) says:
> `_pasteRouteToE80` (nmOpsE80.pm:833) reads `$item->{members}`

But the CURRENT code at nmOpsE80.pm:833 already reads `$item->{route_points}`.
The analysis text was carried over from Cycle 7 and was not updated for Cycle 8.

This means:
- The `members` → `route_points` fix at line 833 WAS applied at some point
  (T1430 checkpoint says "§3.0c/§5.6 fixes applied")
- Cycle 8 §3.3 still FAILED, but for a reason NOT yet identified
- The proposed resolution in the Issues section is therefore WRONG about the
  root cause of the Cycle 8 failure

The actual root cause of the Cycle 8 §3.3 FAIL has not been determined.
Before writing any code fix, the real cause must be established.

---

## What has NOT been done

- §3.3 code bug fix (not started; root cause analysis incomplete)
- Cycle 9 test run (navMate needs restart first)
- open_bugs.md and todo.md not reviewed this session

---

## How to continue

1. Investigate the actual Cycle 8 §3.3 failure root cause:
   - Code at nmOpsE80.pm:833 already has `route_points` -- so what caused
     "0 route_waypoints" in Cycle 8?
   - Candidate: SS10.10 check fires (WPs not found in wpmgr) and returns
     early, but then why is the route created with 0 WPs vs. not created?
   - Candidate: clipboard item for the route has empty route_points despite
     the key being correct -- why?
   - Need to trace the full path from _snapshotDBNode through _doPaste to
     _pasteRouteToE80 looking for where route_points could become empty.
2. Present finding to Patrick; get fix authorized.
3. Apply fix, restart navMate, run Cycle 9 from §1.
