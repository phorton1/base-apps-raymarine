## 2026-05-07T1704 — nmOps runbook written; alpha test run imminent

### What happened this session

nmOps_testplan.md was reviewed and found complete and internally consistent.

nmOps_testplan_runbook.md was written as a memory file to
`C:\Users\Patrick\.claude\projects\C--base-apps-raymarine\memory\`.

UUID table was fully resolved via live /api/nmdb queries (C:/temp/nm_query.pl scripts,
three query passes). All [TBD] entries are now filled in.

### Key discoveries from DB queries

- `node_type` field (not `type`) distinguishes branches from groups in schema 10 collections.
- Navigation/test sub-branch (474e758e...) no longer exists -- gone since schema update.
  [DST] is now `AguaAndTobobe c 2011-03-06` (6f4e72ceae0264de) -- confirmed empty.
- All UUIDs in navmate_db_structure.md memory are stale -- schema update regenerated them.
  The runbook UUID table is the new authoritative source for test node UUIDs.
- The only WPs that satisfy "parent=branch, not in any route" are in the oldE80/Tracks
  branch (2b4e3308ca00cf66): BOCAS1/BOCAS2/TOBOBE series. These are [IsolatedWP1/2/3].
- [SafeBranch] changed from MandalaLogs to "Before Sumwood Channel" (0a4e9820cc015cae)
  because MandalaLogs contains [TestTrack] -- deleting it in §2.7 would break §2.12/§4.3.
- [UnsafeBranch] confirmed: oldE80/Groups (b84e8c3c51009446) -- 62 descendant WPs in
  external routes.
- §2.4/§2.5 ordering conflict: both need a group with no route refs, but §2.5 and §5.7
  also need Bocas. Resolution: §2.4 uses [GroupNoRoute_Dissolve] = Places/Part1 group
  (4e4e405a08033af4). This preserves Bocas for §2.5 and §5.7.
- §3.13 (paste new group to E80) also needs Bocas -- mark NOT_RUN if §2.5 ran first.
  Run §5.7 and §3.13 before §2.5 in the same cycle, or reset between section runs.

### Temp scripts

Two Perl query scripts written to C:/temp/nm_query.pl during this session.
These are throwaway analysis tools; the runbook does not depend on them.
Patrick noted concern about inconsistent temp folder choices -- to be addressed.

### State at checkpoint

- nmOps_testplan_runbook.md: complete, all UUIDs resolved, in memory
- MEMORY.md: updated with pointer to runbook
- session_state.md: NOT yet updated (no code changes this session)
- Alpha test run of runbook about to begin

### What to watch for in alpha run

- §2.4 [GroupNoRoute_Dissolve]: the Places/Part1 group (4e4e405a08033af4) may not be
  selectable if the Part1 branch is collapsed in the UI -- lazy loading may require
  expanding the tree first.
- §2.14 route point keys: format `rp:f34efdd6070022e8:WP_UUID` -- verify the server
  accepts this key format for the new route UUID.
- §5.7 ancestor-wins: Bocas group (a74e90d60300a434) must be intact; run before §2.5.
- The ProgressDialog wait snippet is PowerShell -- confirm it works in the test environment.
- outcome=reject not yet implemented; §5.8 and §5.13 will be NOT_RUN.
