## 2026-05-08T0834 — docs updated; ready to run test cycle

### State at this checkpoint

**Two uncommitted code fixes in the working tree (not yet committed):**
- `nmOps.pm`: added missing `_newNavUUID` sub -- was causing fatal crash on any PASTE_NEW
  to E80 (hung ProgressDialog, blocked Wx loop)
- `nmOpsDB.pm`: fixed Perl map-precedence bug in DELETE_BRANCH handler (~line 487) --
  three consecutive `map { } ARRAY,` without inner parens; wrapped each in parens

**navMate needs a restart** before the test cycle. The two fixes above are in the working
tree but the running instance does not have them yet.

**testplan and runbook are fully updated this session:**
- `nmOps_testplan.md`: rewritten as human-readable prose -- no curl commands, no [Name]
  tokens; CTX_CMD_ prefix dropped to bare names (COPY, PASTE_NEW, DELETE_BRANCH, etc.);
  §2.4 clarified to use a different group than §2.5
- `nmOps_testplan_runbook.md` (memory): all `[CTX_CMD_*]` bracket tokens in curl commands
  replaced with bare names; CTX_CMD constants table reformatted to match

**_nm_groups.pl moved to permanent home:**
- Now at `apps/navMate/_nm_groups.pl` (git-tracked, durable)
- Needs a runbook toolbox entry (see below)

### What to do next (primary Claude directive)

1. Confirm navMate has been restarted (ask Patrick if unsure)
2. Add `_nm_groups.pl` to the runbook toolbox section:
   - Call: `curl -s "http://localhost:9883/api/nmdb" | perl apps/navMate/_nm_groups.pl`
   - Returns: table of non-Navigation/Routes groups with member count, route-ref count, path
3. Run the full test cycle by following the runbook in memory:
   `nmOps_testplan_runbook.md` -- start at §1 Reset, record wall-clock start time,
   proceed through §2, §3, §4, §5; write last_testrun.md at the end
4. The UUID table in the runbook is verified and current (2026-05-08 from live /api/nmdb)
   No re-derivation needed unless Patrick says the baseline DB changed

### Filesystem / temp rules (new this session -- global memory)

- `$temp_dir` = `C:\base_data\temp\raymarine` for transient scratch files
- Durable tools → `apps/navMate/_name.pl` with _ prefix, git-tracked
- Never create or delete folders without explicit permission (global prime directive)
