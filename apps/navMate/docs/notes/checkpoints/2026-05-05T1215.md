## Session 2026-05-05T1215 — generic window editors design start

### What happened

Bootstrap complete. Session started, session_state.md updated to primary_session_started: 2026-05-05T1200.
No prior checkpoint files existed; all timestamps were at 2026-05-05T0000 (initial setup).

Patrick chose to start on [generic window editors] from design_vision.md.

Read: ui_model.md, winDatabase.pm, winE80.pm in full.

### Key code facts noted

winDatabase right pane is already a `detail_panel` (Wx::Panel) containing:
- `color_panel` — small panel with "Color:" label + 40px TextCtrl, hidden except for tracks
- `detail` TextCtrl — multiline, read-only, monospaced, fills the rest

The color TextCtrl already auto-saves to DB on valid 8-hex-char input (_onColorEdit).
That's the established save pattern.

winE80 right side is a bare `detail` TextCtrl directly on the splitter — no panel wrapper.

### Layout decision made

Right pane will be a vertical inner splitter:
- Top: new editor panel (editable fields — replaces/subsumes the current color_panel)
- Bottom: existing read-only detail TextCtrl (unchanged)

### Open questions — not yet answered, need answers before coding

1. winE80 too, or winDatabase only first?
   (E80 is read-only by design; editing would require wire commands)

2. Which DB fields to expose as editable?
   - All types: name, comment
   - Waypoints: lat/lon, color, sym, depth_cm, wp_type
   - Routes: color
   - Tracks: color (already working)
   - Collections: name, comment
   Any to exclude or defer?

3. Save trigger: auto-save as-you-type (current color pattern) or explicit Save button?

4. Persist inner splitter sash position in ini file?

### Next step

Patrick will answer the four design questions, then coding begins.
No code has been written yet.
