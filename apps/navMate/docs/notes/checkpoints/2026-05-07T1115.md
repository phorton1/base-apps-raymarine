# Checkpoint 2026-05-07T1115

---

## Change applied

### c_db.pm $db_def — wp_type default corrected
`wp_type TEXT NOT NULL DEFAULT 'label'` corrected to `DEFAULT 'nav'`.
Resolved in favor of the database — 'nav' is the correct default.

## Open item — second code/DB default discrepancy

Patrick is aware of a second discrepancy of the same kind: a field default or value that
differs between the $db_def code definition and what the database actually holds, also to
be resolved in favor of the database. Field not yet named. Add to todo.md as:

  "Identify and fix second $db_def default discrepancy (resolve in favor of DB, like wp_type fix)"
