## Session 2026-05-05T1240 — sym field decision + schema note

### sym removed from waypoint editor

Patrick corrected the editable fields list for waypoints.
sym is E80-specific and should not be in the navMate DB schema at all.
It will be removed from the schema in a future cleanup pass (requires
$SCHEMA_VERSION bump and migration). Not doing that now.

Editable waypoint fields are now:
  name, comment, lat, lon, wp_type, color, depth_cm

sym is excluded from the property grid entirely (not even display-only).
Or if displayed, display-only with a note — TBD when coding starts.

### Still open — widget/control choices

- Property grid widget: Wx::grid::Grid vs scrolled panel of FlexGridSizer rows
- wp_type (enum: nav/label/sounding) — Choice control vs TextCtrl
- color (8-hex aabbggrr) — TextCtrl with validation (same pattern as existing)
- lat/lon — TextCtrl with decimal degree validation
