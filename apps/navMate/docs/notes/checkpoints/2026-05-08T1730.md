# Checkpoint 2026-05-08T1730

## Session context

Continuing primary Claude session started 2026-05-08T1120. This checkpoint covers
the implementation pass that followed the design review captured in T1439 and T1600.

## What was done this session

All 7 gaps from the T1600 gap list were implemented:

### C5 -- nmOps.pm `_doPaste` Step 4 added
New SS10.1 Step 4 universal route dependency check inserted after Step 3 (recursive
check), before the E80/DB branch split. Collects clipboard WP UUIDs (waypoints +
group members). For E80 destination: checks wpmgr->{waypoints}. For DB destination:
opens a separate connectDB() and calls getWaypoint(). Rejects with named error if
any route member UUID is absent from both destination and clipboard.

### C6 -- nmOps.pm `buildContextMenu` SS10.10 guard extended
Guard now suppresses both PASTE and PASTE_NEW (not just PASTE) when any route in
clipboard has member WPs absent from E80. Condition extended with OR for PASTE_NEW;
filter at end removes both IDs.

### Step labels renumbered in nmOps.pm
E80-block steps 4-7 (branch dissolution, homogeneity, intra-clipboard name collision,
E80-wide name collision) renamed to Steps 5-8 to match updated nmOperations.md spec.

### I1 -- nmOpsDB.pm `_pasteItemsToCollection` ordering sort added
At function entry, when source eq 'e80': builds @ordered_items with non-route types
first, then routes. DB-source pastes unchanged. The for loop now iterates @ordered_items.

### C1 -- nmOpsDB.pm `_pasteItemsToCollection` fresh route branch fixed
Removed the _insertFreshWaypoint loop for route_points. Now generates a fresh route
UUID and calls appendRouteWaypoint with $_->{uuid} for each route_point directly.
Pre-flight Step 4 guarantees those WP UUIDs exist at the destination.

### C2 -- nmOpsDB.pm `_pasteItemsToCollection` non-fresh else route branch fixed
Removed all _pasteOneWaypointToDB calls and the $route_policy tracking logic. Route
branch now directly inserts/updates the route record and rebuilds route_waypoints
using $rp->{uuid} from @$route_points. Cut still handled at the route level only.

### C4 -- nmOpsDB.pm `_pasteDB` PASTE_BEFORE/AFTER fresh route branch fixed
Same fix as C1: removed _insertFreshWaypoint loop; insertRoute + appendRouteWaypoint
with $_->{uuid} from route_points.

### C3 -- nmOpsE80.pm `_pasteNewRouteToE80` rewritten
No longer creates waypoints. Generates a fresh route UUID only. Builds @wp_uuids
from map { $_->{uuid} } @$members. Progress total changed from scalar(@$members)+1
to 1. _pasteNewAllToE80 route total also corrected to 1.

## Gaps NOT fixed in this pass (per T1600 gap list)

- G1: shallow recursive check in _doPaste Step 3 (ancestry not walked)
- G2: _pasteBeforeAfterE80 drops route_point clipboard items silently
- G3: _pasteBeforeAfterE80 PASTE_NEW treats route_point same as waypoint

## Known design ambiguity noted (not resolved)

PASTE_NEW + E80-source + mixed clipboard (WPs + route) to DB destination: Step 4
passes (WP UUIDs are in clipboard), but during execution WPs get fresh UUIDs via
_insertFreshWaypoint, so the route's $rp->{uuid} references (original E80 UUIDs)
won't match the newly created DB records. This combination is unusual and not in
the test cycle. Flagged for Patrick's awareness.

## State

All changes are in: nmOps.pm, nmOpsDB.pm, nmOpsE80.pm.
nmClipboard.pm and nmOperations.md not modified (no changes needed).
navMate requires restart. Cycle 8 needed from §1 to verify all fixes.
